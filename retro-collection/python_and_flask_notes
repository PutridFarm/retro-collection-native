Back-end Development with python and flask

install python3 - comes with pip

pip install pipenv
pipenv install flask flask-sqlalchemy

set FLASK_APP=api
set FLASK_DEBUG=1

pipenv shell
flask run

python
>> from api.models import Movie
>> from api.models import db, create_app
>> db.create_all(app=create_app())

add sqlite3 to C:/sqlite3 and Environment Path Variable